# =============================================================================
# GUARDIAN - CONFIGURACIÓN DE ENTORNO
# =============================================================================
# Copia este archivo a .env y configura tus valores

# =============================================================================
# AUTENTICACIÓN Y SEGURIDAD
# =============================================================================
# Usuario administrador del sistema
GUARDIAN_USER=admin

# Contraseña del administrador (recomendado: usar hash con bcrypt)
GUARDIAN_PASS=mi_password_super_segura

# Clave secreta para JWT (genera una única para tu instalación)
# Puedes generar una con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=tu_clave_secreta_jwt_aqui

# =============================================================================
# CONFIGURACIÓN DE RED
# =============================================================================
# Rango de red por defecto para escanear (formato CIDR)
DEFAULT_NETWORK_RANGE=192.168.0.0/24

# =============================================================================
# NOTIFICACIONES POR EMAIL (OPCIONAL)
# =============================================================================
# Habilitar/deshabilitar notificaciones por email
EMAIL_NOTIFICATIONS=false

# Configuración SMTP para Gmail
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tu_email@gmail.com
SMTP_PASSWORD=tu_app_password_de_gmail

# Email donde recibir las notificaciones
NOTIFICATION_EMAIL=admin@tu-dominio.com
FROM_EMAIL=guardian@tu-dominio.com

# =============================================================================
# CONFIGURACIÓN AVANZADA (OPCIONAL)
# =============================================================================
# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Intervalo de escaneo por defecto en minutos
DEFAULT_SCAN_INTERVAL=30

# =============================================================================
# NOTAS DE CONFIGURACIÓN
# =============================================================================
# 
# Para Gmail:
# 1. Activa la autenticación de 2 pasos
# 2. Genera una "Contraseña de aplicación" específica
# 3. Usa esa contraseña en SMTP_PASSWORD (no tu contraseña normal)
#
# Para otros proveedores de email:
# - Outlook/Hotmail: smtp.live.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - Consulta la documentación de tu proveedor
#
# Seguridad:
# - NUNCA subas el archivo .env a control de versiones
# - Usa contraseñas únicas y seguras
# - Considera usar variables de entorno del sistema en producción